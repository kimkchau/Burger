<li>
	{{burger_name}}

	<button class="change-devour btn btn-outline-danger btn-sm" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if devoured}}Devour it!{{else}}Not eaten yet!{{/if}}
	</button>
</li>